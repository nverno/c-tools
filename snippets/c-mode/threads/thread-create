# -*- mode: snippet -*-
# name: thread-create
# key: threadc
# --
${1:r} = pthread_create(&${2:tid}, NULL, ${3:thread_fun}, ${4:&thread_args});
if (r != 0) {
    fprintf(stderr, "pthread_create: %s\n", strerror(r));
    exit(EXIT_FAILURE);
}$0